spring.application.name=auth-service

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://postgres:5432/postgres-auth
spring.datasource.username=postgres
spring.datasource.password=1robo1

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=8080

auth.jwt.expiration-minutes=10
auth.jwt.issuer=Trippi
auth.jwt.secret=${JWT_SECRET}

services.user-service.url=${SERVICES_USER_SERVICE_URL:http://user-service:8080}